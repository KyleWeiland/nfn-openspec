gnews>=0.4.0
# nltk ships only under newspaper4k's 'nlp' extra, and article.nlp() needs it.
# fetch_articles.py also imports nltk directly, so it is declared explicitly too.
newspaper4k[nlp]>=0.9.0
nltk>=3.6.6
lxml_html_clean>=0.1.0
googlenewsdecoder>=0.1.0
requests>=2.28.0
